export const lightColors = {
  // Base
  background: "#FFF9F3", // soft cream (main background)
  surface: "#FFFFFF", // card / container backgrounds
  border: "#E5E7EB", // light gray border / dividers

  // Text
  text: "#1E1E1E", // primary text (almost black for readability)
  textSecondary: "#6B7280", // secondary text (subtitles, labels)

  // Brand
  primary: "#E63946", // main brand red (call-to-action, buttons)
  primaryLight: "#FCA5A5", // hover / press / subtle backgrounds
  accent: "#FFB703", // accent yellow (badges, highlights, stars)
  success: "#4CAF50", // success state (order confirmed, green signals)
  warning: "#F59E0B", // warning (low stock, caution)
  error: "#DC2626", // error / danger

  // Specials (restaurant vibe)
  foodHighlight: "#FF7F50", // coral (special deals, promotions)
  drinkHighlight: "#219EBC", // teal (drinks, refresh section)

  // Overlays
  overlay: "rgba(0,0,0,0.4)", // modal, drawer background
};

export const darkColors = {
  // Base
  background: "#121212", // deep charcoal background
  surface: "#1E1E1E", // card / container backgrounds
  border: "#2D2D2D", // subtle borders / dividers

  // Text
  text: "#F3F4F6", // primary text (soft white)
  textSecondary: "#9CA3AF", // secondary text (gray, less emphasis)

  // Brand
  primary: "#F87171", // slightly lighter red for CTA buttons
  primaryLight: "#B91C1C", // pressed state / subtle fill
  accent: "#FFD166", // warm yellow (stands out on dark bg)
  success: "#4ADE80", // success green
  warning: "#FBBF24", // golden warning
  error: "#F87171", // error / alerts

  // Specials
  foodHighlight: "#FF8C66", // coral tone for specials
  drinkHighlight: "#4CC9F0", // brighter teal for drinks

  // Overlays
  overlay: "rgba(0,0,0,0.6)", // modal, drawer dim background
};
